plugins {
    id 'application'
    id 'org.gradle.java.experimental-jigsaw' version '0.1.1'
}

dependencies {
    implementation project(':decoder')

    testImplementation group: 'junit', name: 'junit', version: '4.12'
}

javaModule.name = 'de.avwc.app'

mainClassName = "de.avwc.app.Spiegel"
